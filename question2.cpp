#include<iostream>
#include<string>
using namespace std;
int main()
{
    string str[20],strr[20];
    char=ch;
    int i,len;
    cout<<"Enter the string";
    getline(cin,str);
    ignore(getline);
    len=strlen(str);
    for(i=0;i<len;i++)
    {
        str[i]=strr[n-1-i];
    }
    if(strcmp(str,strr)==0)
        cout<<"String is palindrone";
    else
    {
        cout<<"String is not palindrone";
    }
    return 0;
}